{% extends 'base.html.twig' %}

{% block body %}
    <h1>{{ 'edit_product'|trans }}</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn btn-primary">{{ 'save'|trans }}</button>
    {{ form_end(form) }}

    <a href="{{ path('product_show', { id: product.id }) }}" class="btn btn-secondary">{{ 'back_to_product'|trans }}</a>
{% endblock %}