{% extends 'base.html.twig' %}

{% block body %}
    <h1>Edit Product</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn btn-primary">Save</button>
    {{ form_end(form) }}

    <a href="{{ path('product_show', { id: product.id }) }}" class="btn btn-secondary">Back to Product</a> 
{% endblock %}